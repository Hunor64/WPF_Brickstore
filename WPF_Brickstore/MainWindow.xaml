<Window x:Class="WPF_Brickstore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Brickstore"
        mc:Ignorable="d"
        Title="Brickstroe" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="1" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center">Filter:</TextBlock>
        <TextBox TextAlignment="Center"  x:Name="txtFilter" Grid.Row="1" Grid.Column="1" FontSize="22" TextChanged="Filter" VerticalAlignment="Center"/>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22">Fájl betöltése:</TextBlock>
        <Button x:Name="btnLoad" Content="Betöltés" Click="LoadXaml" Grid.Column="1" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <DataGrid x:Name="dgBrickstore" Grid.ColumnSpan="2" Grid.Row="2"></DataGrid>
    </Grid>
</Window>
